#include <stdlib.h> 

//Allocates memory of [size] size. If it's not possible closes program with
//exit code 1. Complexity O(1)  
void *mallocSafe(size_t size);

//Returns bigger number. Complexity O(1)
int max(int x, int y);
